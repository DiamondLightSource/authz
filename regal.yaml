rules:
  idiomatic:
    no-defined-entrypoint:
      level: ignore
    use-some-for-output-vars:
      level: ignore

  imports:
    unresolved-reference:
      level: error
      except-paths:
        - data.diamond.data
        - data.diamond.data.*
