apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "bundler.fullname" . }}
  labels:
    {{- include "bundler.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "bundler.fullname" . }}
  minReplicas: {{ .Values.bundler.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.bundler.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.bundler.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.bundler.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.bundler.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.bundler.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
