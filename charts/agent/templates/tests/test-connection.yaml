apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opal.fullname" . }}-test-connection"
  labels:
    {{- include "opal.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opal.fullname" . }}:{{ .Values.opal.service.port }}']
  restartPolicy: Never
